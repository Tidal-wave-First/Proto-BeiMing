# Proto-BeiMing 配置
imagination:
  max_size_gb: 1
cortex:
  max_size_gb: 10
  storage_path: "./data/cortex"
sandbox:
  path: "./sandbox"
model:
  local_llm: "qwen2.5:7b"
  vision_llm: "llava:7b"
search:
  backend: "bing_cn"
